# Knowledge Base (KB) server dependencies
# Install with: pip install -r requirements.txt
# If download is slow, use mirror: pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
chromadb>=0.4.0
fastembed>=0.2.0
